import { FabricImage, controlsUtils, util, Canvas } from 'fabric';
import { Theme, applyWallTheme } from '../config/theme';

export class Icon extends FabricImage {
  public planDescription: string = "Допольнительное описание на плане по необходимости";
}